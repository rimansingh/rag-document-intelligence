global:
  scrape_interval:     15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "rag-backend"
    static_configs:
      # Uses container name on the Docker network
      - targets: ["rag-backend:8000"]
    metrics_path: "/metrics/prometheus"
    scrape_interval: 10s